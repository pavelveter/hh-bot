version: "3.9"

services:
  bot:
    build: .
    container_name: hh-bot
    env_file:
      - .env
    ports:
      - "127.0.0.1:8271:8271"  # webhook stays private; nginx proxies /hh-bot to localhost
    restart: unless-stopped
    command: ["uv", "run", "main.py"]
